
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# CI/downloaded APK staging dirs
agente-z-debug-apk/
agente-z-release-apk/
MindMax-V4-*.apk
MindMax-V4-debug.apk
MindMax-V4-release.apk

# Room exported schemas (regenerated on each build; keep locally)
app/schemas/
